services:
  app:
    container_name: pm-mvp
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "${PM_PORT:-8080}:8000"
    env_file:
      - .env
    volumes:
      - ./data:/app/backend/data
